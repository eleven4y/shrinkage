/**
 * Configuration of used server
 * @property {string} name
 * @property {string} host
 * @property {number} port
 */
const config = {
  name: 'express',
  host: 'localhost',
  port: 3001,
};

export default config;